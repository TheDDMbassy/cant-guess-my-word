<% content_for :title, "Showing solution" %>

<%= render 'header_taunt' %>
<div class="md:w-2/3 w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>

  <%= render @solution %>

  <%= link_to 'View leaderboard', solutions_path, class: "underline text-blue-700 ml-36" %>
</div>
